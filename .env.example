# LLM provider (OpenAI-compatible API). Copy to .env and fill in.
LLM_BASE_URL=https://polza.ai/api/v1
LLM_API_KEY=

# Postgres (docker compose)
POSTGRES_USER=triage
POSTGRES_PASSWORD=triage
POSTGRES_DB=triage
POSTGRES_PORT=5432

# Langfuse UI (make up-obs)
LANGFUSE_PORT=3100
